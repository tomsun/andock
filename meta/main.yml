---
galaxy_info:
  author: Tom Sundström
  description: An Ansible role that takes another role as input, runs on a Docker host and produces a Docker image.
  min_ansible_version: 1.2

dependencies: []

# Execute andock once for every role that declare itself dependent on andock.
# Ensures that more than one role can depend on andock within the scope of
# a single `ansible-playbook` execution.
allow_duplicates: yes
